import React, { FC } from 'react'
import { Box } from 'ink'

type DetailProps = {
  id: string
}

const Detail: FC<DetailProps> = ({ id }) => {
  return <Box />
}

module.exports = Detail
export default Detail
